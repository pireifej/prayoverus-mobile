Goal: Perform a comprehensive codebase cleanup, refactoring, and code hygiene pass to align with Apple's App Store & Software Architecture Best Practices (Guideline 2.1(a) Performance & Stability, DRY principles, and Separation of Concerns).Objectives:Eliminate Dead/Redundant Code (YAGNI & DRY):Scan the codebase for unused imports, dead functions, commented-out code, orphaned state variables, and redundant utility functions. Remove them.  Merge duplicate logic (such as repeated API calls or duplicate token parsing logic) into single, reusable utility modules.Clean Architecture & Separation of Concerns:Enforce a clear boundary between UI Components, Authentication Logic, State Management, and API Services.  Keep entry points (e.g., App.js, index.js, or MainViewController) clean. Root components should only handle top-level routing and global providers—not inline data fetching or deep state mutations.Defensive Error Handling & App Store Guideline 2.1(a) Compliance:Wrap all persistent storage operations (Reading/Writing LocalStorage, AsyncStorage, SecureStore, etc.) and JSON parsing operations (JSON.parse) in try/catch blocks.Ensure that if persistent storage contains invalid, missing, or expired tokens on cold start, the app safely resets to an unauthenticated state and redirects to the Login screen instead of throwing an unhandled exception and crashing.Add a global Error Boundary (or fallback error handler) so runtime crashes display a recoverable UI instead of killing the process.Improve Debuggability & Maintainability:Add clear, contextual logging for the app initialization lifecycle (e.g., [Boot] Reading cached session..., [Boot] Session invalid, redirecting to login...).Standardize file structure so logic is easy to locate (e.g., /services, /hooks, /components, /screens, /utils).Instructions:Please outline the exact refactoring plan first.Execute the cleanup, showing which files were updated or deleted.Confirm that app initialization is now 100% resilient against corrupted or expired local storage state on subsequent launches.